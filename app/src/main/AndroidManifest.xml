<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.assignment2___">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Assignment2___"
        android:usesCleartextTraffic="true"> <!-- Add this if your app communicates with HTTP endpoints -->

        <!-- LoginActivity as the entry point -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <!-- NewPostActivity -->
        <activity
            android:name=".NewPostActivity"
            android:exported="true" />

        <!-- RegisterActivity -->
        <activity
            android:name=".RegisterActivity"
            android:exported="true" />

        <!-- CommentsActivity -->
        <activity
            android:name=".CommentsActivity"
            android:exported="false" />

        <!-- ManageUsersActivity -->
        <activity
            android:name=".ManageUsersActivity"
            android:exported="true" />

        <!-- EditUserActivity -->
        <activity
            android:name=".EditUserActivity"
            android:exported="true" />

        <!-- AdminActivity -->
        <activity
            android:name=".AdminActivity"
            android:exported="true" />

        <!-- EditProfileActivity -->
        <activity
            android:name=".EditProfileActivity"
            android:exported="true" />

        <!-- AddUserActivity -->
        <activity
            android:name=".AddUserActivity"
            android:exported="true" />



        <activity android:name=".EditPostActivity"
            android:exported="true" />


    </application>
</manifest>
