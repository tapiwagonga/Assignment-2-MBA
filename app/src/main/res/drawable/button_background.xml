<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <corners android:radius="16dp" /> <!-- Rounded corners -->
    <solid android:color="@color/buttonColor" /> <!-- Button color -->
    <padding
        android:left="16dp"
        android:top="8dp"
        android:right="16dp"
        android:bottom="8dp" />
    <stroke
        android:width="2dp"
        android:color="@android:color/white" /> <!-- Border color -->
</shape>
